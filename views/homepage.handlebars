{{#each blogs as |blog|}}
<div class="container">
  <div class="card">
  <div class="card-body">
    <h5 class="card-title">{{blog.title}}</h5>
    <p class="card-text">Author: {{blog.user.username}}</p>
    <a href="/blog" class="btn btn-primary">View Post</a>
  </div>
</div>
</div>
{{/each}}